import { SidebarHeaderPipe } from './sidebar-header.pipe';

describe('SidebarHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new SidebarHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
