import { PreviewKeyPipe } from './preview-key.pipe';

describe('PreviewKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new PreviewKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
